-- ========================================================================================================================================================================================================
-- PROJECT: Churn Revenue Loss Analytics → Ingest
-- SCHEMA:  Raw (churn_raw)
-- --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
USE churn_raw;

-- ========================================================================================================================================================================================================
-- subscriptions →	traces history of every subscription instance tied to an account
-- --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
CREATE TABLE churn_raw.subscriptions (
    subscription_id    VARCHAR(255),
    account_id         VARCHAR(255),
    start_date         VARCHAR(255),
    end_date           VARCHAR(255),
    plan_tier          VARCHAR(255),
    seats              VARCHAR(255),
    mrr_amount         VARCHAR(255),
    is_trial           VARCHAR(255),
    upgrade_flag       VARCHAR(255),
    downgrade_flag     VARCHAR(255),
    churn_flag         VARCHAR(255),
    billing_frequency  VARCHAR(255),
    auto_renew_flag    VARCHAR(255)
);