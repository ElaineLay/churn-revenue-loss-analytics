-- ========================================================================================================================================================================================================
-- PROJECT: Churn Revenue Loss Analytics → Create
-- SCHEMA:  Raw (churn_raw)
-- --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
USE churn_raw;

-- ========================================================================================================================================================================================================
-- accounts	→	contains metadata about each customer (company) using the SaaS platform
-- --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
CREATE TABLE churn_raw.accounts (
    account_id      VARCHAR(255),
    account_name    VARCHAR(255),
    industry        VARCHAR(255),
    country         VARCHAR(255),
    signup_date     VARCHAR(255),
    referral_source VARCHAR(255),
    plan_tier       VARCHAR(255),
    seats           VARCHAR(255),
    is_trial        VARCHAR(255),
    churn_flag      VARCHAR(255)
);