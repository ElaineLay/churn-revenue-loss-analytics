-- ========================================================================================================================================================================================================
-- PROJECT: Churn Revenue Loss Analytics → Create
-- SCHEMA:  Raw (churn_raw)
-- --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
USE churn_raw;

-- ========================================================================================================================================================================================================
-- feature_usage →	captures product interaction events per subscription
-- --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
CREATE TABLE churn_raw.feature_usage (
    usage_id             VARCHAR(255),
    subscription_id      VARCHAR(255),
    usage_date           VARCHAR(255),
    feature_name         VARCHAR(255),
    usage_count          VARCHAR(255),
    usage_duration_secs  VARCHAR(255),
    error_count          VARCHAR(255),
    is_beta_feature      VARCHAR(255)
);